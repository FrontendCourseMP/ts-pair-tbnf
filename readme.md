TODO
1)ДОрогой дневник, сегодня я молодец, сегодня я перепродал айфон, я крутой

задачи:

1)Дается 2 числа: час прибытия по рассписанию и количество часов опаздания, рассчитайте новое значение прибытия.

Время прибытия по рассписанию и новое указывается в 24-часовом формате. 24 час записывается как 0(22 + 2 = 0)


В коде перед вызовом функции производить парсинг ( проверку и приведение) данных.

12.11.2025

Важное на сегодня!
1. Коммиты должны делать каждый со своего аккаунта, чтобы был виден вклад.
2. Не стоит буквально воспринимать задание по части ФИО
3. Ищите элементы внутри уже найденного.

Сделать:
1. Настроить .gitignore node-modules и dist в репозиторий
2. Настроить конфигурацию чтобы избавиться от d.ts файлов.
3. В корне должен быть package.json . Другое плохо - исправляем.
4. Задачи в модули import.
feat Дамки в тапки.


Задача:

Скобки

Даётся строка, содержащая только символы '(', ')', '{','}', '[', ']', определите, валидла ли она. Стктуру данных через данных класс.

Входная строка валидна, если:

- Открытые скобки должны закрыты скобками того же типа.
- Открытые скобки должны закрываться в правильно порядке.
- Каждой закрывающейся скобке соответствует открытая скобка того же типа.
